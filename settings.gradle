pluginManagement {
    apply from: 'gradle/dependency-versions.gradle'

    plugins {
        id 'com.diffplug.spotless' version pluginVersions.spotless
        id 'com.github.ben-manes.versions' version pluginVersions.versions
        id 'com.google.cloud.tools.jib' version pluginVersions.jib
    }

    resolutionStrategy {
        eachPlugin {
            if (requested.id.id == 'org.springframework.boot') {
                useModule "org.springframework.boot:spring-boot-gradle-plugin:$libraryVersions.springBoot"
            }
        }
    }

    repositories {
        gradlePluginPortal()
        maven { url = 'https://repo.spring.io/libs-milestone/' }
    }
}

rootProject.name = 'traintracker'

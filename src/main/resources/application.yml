spring:
  cache:
    cache-names: voyages-hr
    caffeine:
      spec: maximumSize=100,expireAfterWrite=60s

  jackson:
    default-property-inclusion: non_absent
    serialization:
      write_dates_as_timestamps: false

  thymeleaf:
    mode: HTML

security:
  basic:
    enabled: false
    realm: TrainTracker
